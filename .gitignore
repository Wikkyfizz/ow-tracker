ow_tracker.db
venv/
inbox_queue.json
__pycache__/
*.py[cod]
*.pyo
.env
venv/
.venv/
*.egg-info/
dist/
build/
.pytest_cache/
inbox/
# Hero icons are intentionally committed so the tool works out-of-the-box.
# Regenerate / update them with: python fetch_icons.py

# Disposable mock-render screenshots (regenerate via Edge headless)
docs/mockups/_preview-*.png
